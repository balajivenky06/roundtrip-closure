# Copy this file to `.env` and fill in any keys you actually use.
# The current Ollama-only design does NOT require any API keys.
# These slots are reserved for future closed-weight reference cells
# (deferred to revision rounds per the concept note Q-Q1 decision).

# Together.ai (deferred — not currently used)
TOGETHER_API_KEY=

# OpenAI (deferred — not currently used)
OPENAI_API_KEY=

# Anthropic (deferred — not currently used)
ANTHROPIC_API_KEY=

# Ollama (local; default is correct unless you've remapped the port)
OLLAMA_HOST=http://localhost:11434
