"""scripts/ — runnable entry points (pilot, full sweep, analysis, recovery).

Marks the directory as a proper package so `python3 -m scripts.run_analysis`,
`python3 -m scripts.run_pilot`, etc. work reliably under stricter namespace-
package handling.
"""
