"""
plots/ — publication-quality figure generation.

Each plot script consumes results/results_roundtrip.tsv (loaded via
analyze.load_results) and writes a PNG to plots/output/. Styling is
shared via plots.style.
"""
